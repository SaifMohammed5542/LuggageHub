/** @type {import('next-sitemap').IConfig} */
module.exports = {
  siteUrl: 'https://luggageterminal.com', // Your live site URL
  generateRobotsTxt: true, // Optional: generates robots.txt file too
  changefreq: 'daily',
  priority: 0.7,
  sitemapSize: 5000,
};
